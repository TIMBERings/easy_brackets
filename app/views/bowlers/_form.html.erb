<%= simple_form_for(@bowler) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :starting_lane %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
